// Mock "current user" — in the real app this comes from the JWT/session.
export const CURRENT_USER = 'Usuario Demo';

import { TODAY } from './documents';

/** The upload date used for new mock uploads (base "today"). */
export const UPLOAD_DATE = TODAY;
